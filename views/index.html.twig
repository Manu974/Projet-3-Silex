{% extends "layout.html.twig" %}

{% block title %}Home{% endblock %}

{% block content %}
{% for billet in billets %}
<articl e>
    <h2>{{ billet.title }}</h2>
    <p>{{ billet.content|raw }}</p>
    <p> publié par {{ billet.author.username }}</p>
    <a class="billetTitle" href="{{ path('billet', { 'id': billet.id }) }}">Commentaires</a>
</article>
{% endfor %}
{% endblock %}