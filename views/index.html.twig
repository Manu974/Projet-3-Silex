{% extends "layout.html.twig" %}

{% block title %}Home{% endblock %}

{% block content %}
{% for billet in billets %}
<articl e>
    <h2><a class="billetTitle" href="{{ path('billet', { 'id': billet.id }) }}">{{ billet.title }}</a></h2>
    <p>{{ billet.content }}</p>
</article>
{% endfor %}
{% endblock %}